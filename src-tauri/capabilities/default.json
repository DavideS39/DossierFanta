{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window of DossierFanta M1. Allows the frontend to invoke local Rust commands only — no external network access, no shell, no fs directly.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-start-dragging",
    "core:app:default"
  ]
}
